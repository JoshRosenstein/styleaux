export const color = {
  prop: 'color',
  path: 'palette',
}

export const bgcolor = style({
  prop: 'bgcolor',
  cssProp: 'backgroundColor',
  path: 'palette',
})

const palette = [color, bgcolor]
export default palette
