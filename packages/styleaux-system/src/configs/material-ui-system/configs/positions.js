export const position = {
  prop: 'position',
}

export const zIndex = {
  prop: 'zIndex',
  path: 'zIndex',
}

export const top = {
  prop: 'top',
}

export const right = {
  prop: 'right',
}

export const bottom = {
  prop: 'bottom',
}

export const left = {
  prop: 'left',
}

export default [position, zIndex, top, right, bottom, left]
