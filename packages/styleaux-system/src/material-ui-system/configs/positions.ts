import {positionConfig} from '../../common/positionsConfig'

export const zIndexConfig = {
  prop: 'zIndex',
  path: 'zIndex',
}

export const topConfig = {
  prop: 'top',
}

export const rightConfig = {
  prop: 'right',
}

export const bottomConfig = {
  prop: 'bottom',
}

export const leftConfig = {
  prop: 'left',
}

export {positionConfig}
export const positionsConfig = [
  positionConfig,
  zIndexConfig,
  topConfig,
  rightConfig,
  bottomConfig,
  leftConfig,
]
